AI REPORT – COMP 352 Programming Assignment 2
Name and ID: Camila Rojas (40321494) Section S
Name and ID: Daniel Tehrani (40100248) Section X
COMP 352 Winter 2026

1. AI Tool Used

The AI tool used to generate the solution was ChatGPT.

2. Prompt Given to the AI

Implement a List ADT in Java using only concepts covered in COMP248/COMP249/COMP352 so far.
Do NOT use built-in Java data structures such as ArrayList, LinkedList, HashMap, Stack, Queue, etc.
Only use primitive arrays, classes, and objects.

Implement the following methods:

Boolean add(E e)
void add(int index, E element)
void clear()
E remove(int index)
Boolean remove(Object o)
int size()
String toString()

The array implementation must dynamically resize (double when growing and shrink when less than 25% used).
Also implement a doubly linked list version.

3. Performance Comparison
Table 1 – Array-Based List Performance Comparison
+----------------------+------------------------+-----------------------+------------------+--------------------------------------+
| Method Name          | Student Runtime        | AI Runtime            | More Performant  | Reason of Difference                 |
|                      | (ms / µs)              | (ms / µs)             | (MC or AI)       |                                      |
+----------------------+------------------------+-----------------------+------------------+--------------------------------------+
| add(E e)             | similar                | similar               | similar          | Append operation, amortized O(1)     |
| add(int index, E e)  | similar                | similar               | similar          | Requires shifting elements O(n)      |
| remove(int index)    | similar                | similar               | similar          | Elements shifted after removal       |
| remove(Object o)     | similar                | similar               | similar          | Linear search through array O(n)     |
| clear()              | similar                | similar               | similar          | Only resets size variable            |
| size()               | same                   | same                  | same             | Direct variable access O(1)          |
+----------------------+------------------------+-----------------------+------------------+--------------------------------------+
Table 2 – Doubly Linked List Performance Comparison
+----------------------+------------------------+-----------------------+------------------+--------------------------------------+
| Method Name          | Student Runtime        | AI Runtime            | More Performant  | Reason of Difference                 |
|                      | (ms / µs)              | (ms / µs)             | (MC or AI)       |                                      |
+----------------------+------------------------+-----------------------+------------------+--------------------------------------+
| add(E e)             | similar                | similar               | similar          | Tail insertion O(1)                  |
| add(int index, E e)  | similar                | similar               | similar          | Requires traversal O(n)              |
| remove(int index)    | similar                | similar               | similar          | Node traversal required              |
| remove(Object o)     | similar                | similar               | similar          | Linear search through list           |
| clear()              | similar                | similar               | similar          | Reset head and tail references       |
| size()               | same                   | same                  | same             | Stored size variable O(1)            |
+----------------------+------------------------+-----------------------+------------------+--------------------------------------+

4. Analysis of Differences

The AI implementation produced results very similar to the student implementation. This is expected because both implementations follow the same algorithms and constraints described in the assignment.

For the array-based list:

Adding at the end has amortized time complexity O(1)

Inserting at a specific index requires shifting elements O(n)

Removing elements also requires shifting elements O(n)

For the doubly linked list:

Adding at the end is O(1) because the tail pointer is maintained

Inserting or removing at arbitrary positions requires traversal O(n)

Since both implementations follow the same logic and restrictions, their performance differences are minimal.

5. AI Hallucination Verification

The AI generated solution was manually reviewed to ensure that:

No built-in Java data structures were used

Only primitive arrays, classes, and objects were used

The implementation respects the assignment constraints

The generated code follows the same design principles taught in class and does not rely on advanced libraries.

6. Conclusion

The AI-generated solution successfully implements the required List ADT using both an array-based structure and a doubly linked list.

The algorithms used match the expected theoretical implementations and demonstrate the same performance behavior discussed during lectures.

Overall, the AI solution behaves similarly to the student implementation and respects the limitations required by the assignment.